import React, { useState } from 'react';
import { Type, AlignLeft, Check, X } from 'lucide-react';

export default function App() {
  const [isOpen, setIsOpen] = useState(true);
  const [formData, setFormData] = useState({
    name: '',
    description: ''
  });

  const handleClose = () => setIsOpen(false);

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log('Submitted:', formData);
    // 逻辑完成后关闭
    // handleClose();
  };

  if (!isOpen) return (
    <div className="flex items-center justify-center h-screen bg-slate-50 font-sans">
      <button 
        onClick={() => setIsOpen(true)}
        className="px-6 py-2 bg-white border border-slate-200 text-slate-500 rounded-lg shadow-sm hover:bg-slate-50 transition-colors text-sm font-medium"
      >
        新建作品集
      </button>
    </div>
  );

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900/5 backdrop-blur-[1px] font-sans">
      {/* 极简小弹窗 */}
      <div className="w-full max-w-[280px] bg-white border border-slate-100 rounded-2xl shadow-xl overflow-hidden animate-in fade-in zoom-in-95 duration-200">
        
        <form onSubmit={handleSubmit} className="p-5">
          
          {/* Header - 文字标题 */}
          <div className="pb-4 text-center">
            <h3 className="text-[15px] font-bold text-slate-700">新建作品集</h3>
          </div>

          {/* 表单区域 */}
          <div className="space-y-3">
            
            {/* 名称 (必填) */}
            <div className="flex items-center gap-3 px-3 py-2.5 bg-slate-50 rounded-xl focus-within:ring-1 focus-within:ring-slate-200 focus-within:bg-white transition-all border border-transparent focus-within:border-slate-200">
              <Type className="w-4 h-4 text-slate-400 shrink-0" />
              <input
                type="text"
                required
                placeholder="名称"
                className="w-full bg-transparent text-sm text-slate-700 placeholder:text-slate-300 focus:outline-none"
                value={formData.name}
                onChange={(e) => setFormData({ ...formData, name: e.target.value })}
              />
            </div>

            {/* 描述 (选填) */}
            <div className="flex items-start gap-3 px-3 py-2.5 bg-slate-50 rounded-xl focus-within:ring-1 focus-within:ring-slate-200 focus-within:bg-white transition-all border border-transparent focus-within:border-slate-200">
              <AlignLeft className="w-4 h-4 text-slate-400 shrink-0 mt-0.5" />
              <textarea
                placeholder="描述"
                rows={2}
                className="w-full bg-transparent text-sm text-slate-700 placeholder:text-slate-300 focus:outline-none resize-none leading-relaxed"
                value={formData.description}
                onChange={(e) => setFormData({ ...formData, description: e.target.value })}
              />
            </div>
            
          </div>

          {/* Footer - 居中对称按钮 */}
          <div className="flex items-center gap-2 mt-6">
            <button
              type="button"
              onClick={handleClose}
              className="flex-1 py-2 text-xs font-bold text-slate-400 bg-slate-50 hover:bg-slate-100 rounded-xl transition-all active:scale-95 border border-slate-100"
            >
              取消
            </button>
            <button
              type="submit"
              className="flex-1 py-2 text-xs font-bold text-slate-600 bg-slate-50 hover:bg-slate-100 rounded-xl transition-all active:scale-95 border border-slate-200"
            >
              确认
            </button>
          </div>

        </form>
      </div>
    </div>
  );
}