import React, { useState } from 'react';
import { 
  Globe, 
  Upload, 
  LogOut, 
  Settings, 
  Grid, 
  BookOpen, 
  Users, 
  Mail, 
  Github, 
  Heart, 
  ExternalLink,
  Check,
  X,
  Sparkles,
  ChevronRight
} from 'lucide-react';

export default function SettingsPage() {
  // Navigation active state
  const [activeTab, setActiveTab] = useState('settings');
  
  // Interaction states for demo
  const [showGroupModal, setShowGroupModal] = useState(false);
  const [selectedGroup, setSelectedGroup] = useState('PKU 译介工坊');
  const [toastMessage, setToastMessage] = useState(null);
  const [isUploading, setIsUploading] = useState(false);

  // Group options for mock selector
  const translationGroups = [
    'PKU 译介工坊',
    '北大汉化社',
    '未名翻译组',
    '极客汉化联盟'
  ];

  // Helper to trigger interactive toasts
  const triggerToast = (msg) => {
    setToastMessage(msg);
    setTimeout(() => setToastMessage(null), 3000);
  };

  // High quality structured data for the footer acknowledgements
  const acknowledgments = {
    core: [
      { name: 'Pkuism', link: 'https://github.com/pkuislm', desc: '核心技术支持与底层架构建议' },
    ],
    repositories: [
      { name: 'pku-translation-core', link: 'https://github.com', desc: '文本解析与汉化匹配核心引擎' },
      { name: 'localization-helper', link: 'https://github.com', desc: '多语言翻译资源自动热更新套件' },
    ],
    techStack: [
      { name: 'React', link: 'https://react.dev' },
      { name: 'Tailwind CSS', link: 'https://tailwindcss.com' },
      { name: 'Lucide Icons', link: 'https://lucide.dev' }
    ]
  };

  const handleAvatarUpload = (e) => {
    setIsUploading(true);
    setTimeout(() => {
      setIsUploading(false);
      triggerToast('✨ 头像上传成功！');
    }, 1500);
  };

  const handleLogout = () => {
    triggerToast('🚪 正在安全退出登录...');
  };

  return (
    <div className="flex h-screen w-screen bg-[#fafaf9] overflow-hidden text-slate-800 font-sans antialiased relative selection:bg-emerald-100 selection:text-emerald-900">
      
      {/* Background Dotted Grid - Matching image_b533a1.png exactly */}
      <div 
        className="absolute inset-0 pointer-events-none" 
        style={{
          backgroundImage: 'radial-gradient(#e5e5e0 1.2px, transparent 1.2px)',
          backgroundSize: '24px 24px',
          opacity: 0.8
        }}
      />

      {}
      {/* Sidebar navigation designed to precisely mirror image_b533a1.png */}
      <aside className="w-16 h-full bg-white border-r border-slate-100 flex flex-col justify-between items-center py-4 z-10 shrink-0">
        
        {/* Top App Logo (Green tree/clover icon) */}
        <div className="flex flex-col items-center gap-6 w-full">
          <div className="w-10 h-10 flex items-center justify-center text-[#2e7d32] hover:scale-105 transition-transform cursor-pointer">
            <svg viewBox="0 0 24 24" className="w-8 h-8 fill-current">
              <path d="M12 2C11.5 2 10 5 10 8C10 9.5 10.5 11 12 11C13.5 11 14 9.5 14 8C14 5 12.5 2 12 2Z" />
              <path d="M7.5 10C6 10 3 11.5 3 13C3 14.5 5 16 7.5 16C10 16 11 14 11 13C11 11.5 9 10 7.5 10Z" />
              <path d="M16.5 10C15 10 13 11.5 13 13C13 14 14 16 16.5 16C19 16 21 14.5 21 13C21 11.5 18 10 16.5 10Z" />
              <path d="M12 12C11.5 12 11 14 11 17C11 20 11.5 22 12 22C12.5 22 13 20 13 17C13 14 12.5 12 12 12Z" />
            </svg>
          </div>

          {/* Navigation Action Buttons Group */}
          <div className="flex flex-col gap-4 w-full items-center">
            
            {/* Globe Tab (Active in image) */}
            <button 
              onClick={() => setActiveTab('globe')}
              className={`w-10 h-10 rounded-lg flex items-center justify-center transition-all ${
                activeTab === 'globe' 
                  ? 'bg-[#8ba870] text-white shadow-sm shadow-[#8ba870]/30' 
                  : 'text-slate-400 hover:bg-slate-50 hover:text-slate-600'
              }`}
            >
              <Globe size={20} strokeWidth={2} />
            </button>

            {/* Grid Icon */}
            <button 
              onClick={() => setActiveTab('grid')}
              className={`w-10 h-10 rounded-lg flex items-center justify-center transition-all ${
                activeTab === 'grid' 
                  ? 'bg-[#8ba870] text-white shadow-sm' 
                  : 'text-slate-400 hover:bg-slate-50 hover:text-slate-600'
              }`}
            >
              <Grid size={20} strokeWidth={2} />
            </button>

            {/* Book Icon */}
            <button 
              onClick={() => setActiveTab('book')}
              className={`w-10 h-10 rounded-lg flex items-center justify-center transition-all ${
                activeTab === 'book' 
                  ? 'bg-[#8ba870] text-white shadow-sm' 
                  : 'text-slate-400 hover:bg-slate-50 hover:text-slate-600'
              }`}
            >
              <BookOpen size={20} strokeWidth={2} />
            </button>

            {/* People Icon */}
            <button 
              onClick={() => setActiveTab('people')}
              className={`w-10 h-10 rounded-lg flex items-center justify-center transition-all ${
                activeTab === 'people' 
                  ? 'bg-[#8ba870] text-white shadow-sm' 
                  : 'text-slate-400 hover:bg-slate-50 hover:text-slate-600'
              }`}
            >
              <Users size={20} strokeWidth={2} />
            </button>

            {/* Mail Icon with Red Dot Alert */}
            <button 
              onClick={() => setActiveTab('mail')}
              className={`w-10 h-10 rounded-lg flex items-center justify-center relative transition-all ${
                activeTab === 'mail' 
                  ? 'bg-[#8ba870] text-white shadow-sm' 
                  : 'text-slate-400 hover:bg-slate-50 hover:text-slate-600'
              }`}
            >
              <Mail size={20} strokeWidth={2} />
              <span className="absolute top-2.5 right-2.5 w-2 h-2 bg-red-500 rounded-full border border-white" />
            </button>

          </div>
        </div>

        {/* Bottom Gear Icon (Settings - Active with left green indication bar) */}
        <div className="w-full flex items-center justify-center relative">
          {/* Active green indicator on the far left edge */}
          <div className="absolute left-0 top-1/2 -translate-y-1/2 w-[3px] h-8 bg-[#2e7d32] rounded-r-md" />
          
          <button 
            onClick={() => setActiveTab('settings')}
            className={`w-10 h-10 rounded-lg flex items-center justify-center transition-all ${
              activeTab === 'settings' 
                ? 'text-[#2e7d32] bg-emerald-55/40 font-bold' 
                : 'text-slate-400 hover:text-slate-600'
            }`}
          >
            <Settings size={20} strokeWidth={2} className={activeTab === 'settings' ? 'animate-spin-slow' : ''} />
          </button>
        </div>

      </aside>

      {}
      {/* Central content view containing setting modules and the newly requested interactive footer */}
      <main className="flex-1 flex flex-col justify-between items-center relative overflow-y-auto px-6 py-10 z-10">
        
        {/* Placeholder spacer to push content naturally to center vertically while holding room */}
        <div className="flex-1 flex flex-col justify-center items-center w-full max-w-xl py-8">
          
          {/* Settings Section Header (Subtle) */}
          <div className="text-center mb-6">
            <span className="text-xs font-semibold tracking-wider text-emerald-700 bg-emerald-50 px-3 py-1 rounded-full uppercase">
              系统设置
            </span>
          </div>

          {/* Cards Container */}
          <div className="w-full flex flex-col gap-4">
            
            {/* Card 1: Switch Translation/Localization Group */}
            <div 
              onClick={() => setShowGroupModal(true)}
              className="bg-white border border-slate-100/80 hover:border-slate-200 rounded-xl px-6 py-4 flex items-center justify-between shadow-[0_2px_8px_rgba(0,0,0,0.02)] hover:shadow-[0_4px_16px_rgba(0,0,0,0.04)] transition-all cursor-pointer group"
            >
              <div className="flex items-center gap-3">
                <span className="text-base font-medium text-slate-800 tracking-wide">
                  切换汉化组
                </span>
                <span className="text-xs bg-slate-50 text-slate-500 px-2 py-0.5 rounded-md border border-slate-100 group-hover:bg-emerald-50 group-hover:text-emerald-700 group-hover:border-emerald-100 transition-colors">
                  当前: {selectedGroup}
                </span>
              </div>
              <div className="text-slate-400 group-hover:text-slate-600 group-hover:translate-x-0.5 transition-all">
                <Globe size={18} strokeWidth={1.8} />
              </div>
            </div>

            {/* Card 2: Upload Avatar */}
            <label 
              className="bg-white border border-slate-100/80 hover:border-slate-200 rounded-xl px-6 py-4 flex items-center justify-between shadow-[0_2px_8px_rgba(0,0,0,0.02)] hover:shadow-[0_4px_16px_rgba(0,0,0,0.04)] transition-all cursor-pointer group"
            >
              <input 
                type="file" 
                accept="image/*" 
                className="hidden" 
                onChange={handleAvatarUpload} 
                disabled={isUploading}
              />
              <div className="flex items-center gap-3">
                <span className="text-base font-medium text-slate-800 tracking-wide">
                  上传头像
                </span>
                {isUploading && (
                  <span className="text-xs text-amber-600 animate-pulse">正在处理图片...</span>
                )}
              </div>
              <div className="text-slate-400 group-hover:text-slate-600 group-hover:-translate-y-0.5 transition-all">
                <Upload size={18} strokeWidth={1.8} />
              </div>
            </label>

            {/* Card 3: Logout */}
            <div 
              onClick={handleLogout}
              className="bg-white border border-slate-100/80 hover:border-slate-200 rounded-xl px-6 py-4 flex items-center justify-between shadow-[0_2px_8px_rgba(0,0,0,0.02)] hover:shadow-[0_4px_16px_rgba(0,0,0,0.04)] transition-all cursor-pointer group"
            >
              <span className="text-base font-medium text-slate-800 tracking-wide">
                退出登录
              </span>
              <div className="text-slate-400 group-hover:text-red-500 group-hover:translate-x-1 transition-all">
                <LogOut size={18} strokeWidth={1.8} />
              </div>
            </div>

          </div>

        </div>

        {/* Ultra-compact minimalist footer to preserve the spaciousness of image_b533a1.png */}
        <footer className="w-full max-w-xl mt-auto pt-6 text-center z-10">
          <div className="flex flex-wrap items-center justify-center gap-x-2.5 gap-y-1 text-[11px] text-slate-400">
            <span>致谢:</span>
            
            {/* 1. Primary User Mention: Pkuism */}
            <a 
              href="https://github.com/pkuislm" 
              target="_blank" 
              rel="noopener noreferrer"
              className="text-slate-500 hover:text-emerald-700 transition-colors font-medium hover:underline inline-flex items-center gap-0.5"
              title="前往 Pkuism GitHub 个人主页"
            >
              Pkuism
              <ExternalLink size={9} className="opacity-60" />
            </a>
            
            <span className="text-slate-200 select-none">•</span>

            {/* 2. Core Repository */}
            <a 
              href="https://github.com" 
              target="_blank" 
              rel="noopener noreferrer"
              className="text-slate-500 hover:text-emerald-700 transition-colors hover:underline"
            >
              pku-translation-core
            </a>

            <span className="text-slate-200 select-none">•</span>

            {/* 3. Helper Repository */}
            <a 
              href="https://github.com" 
              target="_blank" 
              rel="noopener noreferrer"
              className="text-slate-500 hover:text-emerald-700 transition-colors hover:underline"
            >
              localization-helper
            </a>
          </div>
        </footer>

      </main>

      {/* Nice interactive experience explaining modal setups when clicking group switch card */}
      {showGroupModal && (
        <div className="fixed inset-0 bg-slate-900/40 backdrop-blur-sm flex items-center justify-center z-50 p-4 transition-all animate-fade-in">
          <div className="bg-white rounded-2xl max-w-sm w-full shadow-xl border border-slate-100 p-6 flex flex-col gap-4 transform transition-all scale-100">
            
            {/* Modal Header */}
            <div className="flex items-center justify-between">
              <div className="flex items-center gap-2">
                <Globe className="text-emerald-600 w-5 h-5" />
                <h3 className="text-lg font-bold text-slate-800">选择活跃汉化组</h3>
              </div>
              <button 
                onClick={() => setShowGroupModal(false)}
                className="text-slate-400 hover:text-slate-600 hover:bg-slate-100 p-1 rounded-lg transition-colors"
              >
                <X size={18} />
              </button>
            </div>

            <p className="text-xs text-slate-500">
              切换到以下支持汉化的翻译小组。切换后系统文本、注释等汉化成果会进行针对性载入。
            </p>

            {/* Selection Options List */}
            <div className="flex flex-col gap-2 my-2">
              {translationGroups.map((group) => (
                <button
                  key={group}
                  onClick={() => {
                    setSelectedGroup(group);
                    setShowGroupModal(false);
                    triggerToast(`🎉 已成功切换至「${group}」！`);
                  }}
                  className={`flex items-center justify-between px-4 py-3 rounded-xl border text-left text-sm font-medium transition-all ${
                    selectedGroup === group 
                      ? 'border-emerald-500 bg-emerald-50/40 text-emerald-800 shadow-sm' 
                      : 'border-slate-100 bg-slate-50/50 text-slate-700 hover:bg-slate-50 hover:border-slate-200'
                  }`}
                >
                  <span>{group}</span>
                  {selectedGroup === group && (
                    <Check size={16} className="text-emerald-600" />
                  )}
                </button>
              ))}
            </div>

            <div className="text-[11px] text-slate-400 bg-slate-50 p-2.5 rounded-lg border border-slate-100 flex items-center gap-2">
              <span className="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-ping shrink-0" />
              <span>
                如果您所在的翻译组未被列出，请提交 Pull Request 至核心代码仓。
              </span>
            </div>

          </div>
        </div>
      )}

      {}
      {/* Toast popup for smooth click responses instead of using illegal alerts */}
      {toastMessage && (
        <div className="fixed bottom-6 right-6 bg-slate-900 text-white px-4 py-3 rounded-xl shadow-lg flex items-center gap-2 z-50 animate-bounce-subtle border border-slate-800/50">
          <div className="w-2 h-2 rounded-full bg-emerald-400" />
          <span className="text-xs font-semibold tracking-wide">{toastMessage}</span>
        </div>
      )}

      {/* Extra helper styles for subtle animations */}
      <style>{`
        @keyframes fadeIn {
          from { opacity: 0; }
          to { opacity: 1; }
        }
        .animate-fade-in {
          animation: fadeIn 0.15s ease-out forwards;
        }
        @keyframes spinSlow {
          from { transform: rotate(0deg); }
          to { transform: rotate(360deg); }
        }
        .animate-spin-slow {
          animation: spinSlow 8s linear infinite;
        }
        @keyframes bounceSubtle {
          0%, 100% { transform: translateY(0); }
          50% { transform: translateY(-3px); }
        }
        .animate-bounce-subtle {
          animation: bounceSubtle 1.5s ease-in-out infinite;
        }
      `}</style>

    </div>
  );
}
