        <button
          onClick={() => setIsOpen(!isOpen)}
          className={clsx(
            "w-12 h-12 flex items-center justify-center",
            "border-2 rounded-lg transition-all duration-300",
            isOpen
              ? "bg-black border-black text-white shadow-lg rotate-45"
              : "bg-white border-gray-200 text-gray-700 hover:border-black hover:shadow-sm",
          )}
          aria-label="Toggle Menu"
        >
